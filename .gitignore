.DS_Store
.playwright-mcp/
*.log

# Local reference: original Framer (with-runtime) pages, for rebuilding animations
framer-reference/

# Local-only faithful reference mirror (copyrighted third-party content, never commit/deploy)
reference-mirror/

# site/ is the deployed web root. It was quarantined while it still carried
# third-party content; it is committed now that every page is SMAG's own
# copy and every asset is referenced by a page (tools/rebuild/prepare_deploy.py).
__pycache__/
